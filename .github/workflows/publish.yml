name: Publish

on:
  push:
    tags:
      - "v*"

# No concurrency group here on purpose: a shared fixed group only keeps the latest
# pending run, so a release queued behind CI could be cancelled by a newer CI run.
# Concurrent demo-account collisions (error 468) are handled by the retry in the tests.

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"
      - run: pnpm install --frozen-lockfile
      - name: Set version from tag
        run: npm pkg set version="${GITHUB_REF_NAME#v}"
      - run: pnpm build
      - run: pnpm test
      - name: Publish to NPM
        run: pnpm publish --access public --no-git-checks --provenance
